# Copy to android/keystore.properties and fill in. NEVER commit the real file
# or the .jks it points at -- both are gitignored.
storeFile=/Users/tom/quahuru-upload.jks
storePassword=CHANGE_ME
keyAlias=quahuru
keyPassword=CHANGE_ME
