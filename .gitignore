*.ckpt
*.jsonl
*.npz
*.pkl
*.pt
*.pth
.DS_Store
.venv/
.ipynb_checkpoints/
__pycache__/
replays/
training_data/
weights*.json
opponent_model.txt
_attic/
# Derived test fixtures, regenerated on demand by their dump_*.py scripts.
# NB a trailing "# comment" on a gitignore line is NOT a comment -- it is part
# of the pattern, so these have to sit on their own lines. The encoder line
# carried one for weeks and silently matched nothing.
encoder_fixture.json
engine_fixture.json
heuristic_fixture.json
agent_fixture.json
trace_fixture.json
ort/*.gz    # generated at image build time (see Dockerfile)
